---

- hosts: localhost
  gather_facts: false
  roles:
    - kubesphere-defaults
    - role: ks-devops
      when:
        - "status.devops is not defined or status.devops.status is not defined or status.devops.status != 'enabled'"
